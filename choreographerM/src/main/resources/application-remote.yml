spring:
  kafka:
    consumer:
      group-id: "default"

  datasource:
    driver-class-name: org.postgresql.Driver
#    url: jdbc:postgresql://host.docker.internal:5432/e-market
    url: jdbc:postgresql://localhost:5432/e-market
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQL82Dialect
  topic-order:
    name: t.product.create-order
  topic-order-created:
    name: t.product.order-created
  topic-check-product:
    name: t.product.check-product
  topic-product-checked:
    name: t.product.product-checked
server:
  port: 8099

